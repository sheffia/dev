import { TaskfilterrvPipe } from './taskfilterrv.pipe';

describe('TaskfilterrvPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskfilterrvPipe();
    expect(pipe).toBeTruthy();
  });
});
